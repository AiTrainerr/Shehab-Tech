const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { let retries = 5; while(retries > 0) { try { const count = await prisma.projectSentence.count({ where: { speakerCode: 'G0274' } }); console.log('Total sentences for G0274:', count); const recs = await prisma.voiceRecording.findMany({ where: { user: { email: 'abuzaidnoor472@gmail.com' } }, include: { sentence: true } }); console.log('Total recordings for abuzaidnoor:', recs.length); const sentenceCounts = {}; recs.forEach(r => { if (!sentenceCounts[r.sentence.order]) sentenceCounts[r.sentence.order] = 0; sentenceCounts[r.sentence.order]++; }); const dups = Object.keys(sentenceCounts).filter(k => sentenceCounts[k] > 1); console.log('Sentences with duplicate recordings:', dups.length); return; } catch(e) { console.error('DB Error, retrying...'); retries--; await new Promise(r => setTimeout(r, 2000)); } } } main().finally(() => prisma.$disconnect());
